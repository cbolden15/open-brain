# m1-search session

Preferred model: `gpt-6-astra`; reasoning: high. Confirm this model is available on the Mac mini before launching. Do not silently substitute or inherit an unknown default.

# Phase task

Objective: Implement M1 T01-T02 only: fix complete ordered multi-source provenance reconciliation and add composed public search regression tests. No schema migration or broader retrieval/lifecycle work.
Source of truth and precedence: Read this first: docs/ai/kickoffs/mac-mini-new-user/README.md, then docs/plans/2026-09-16-new-user-functionality.md and its coverage JSON. Ground actual repository root, branch, HEAD, dirty state and CLAUDE.md. Resolve historical laptop absolute paths by their repository-relative suffix in your own checkout; do not use old /tmp paths or treat an old handoff SHA as current. Start only after the coordinator assignment names your branch, base SHA and exclusive files. Reproduce the first two-source approval and cumulative source update through public app search, including unrelated query failure, before fixing.
Allowed write scope: Only coordinator-approved reconciliation/resolver implementation and M1 engine/app integration test files in your m1-search worktree, plus your generated workstream report. Preserve provenance validation, trust/space/path identity, redaction, complete membership, legacy single-source handling and existing export/doctor invariants. Never edit schema, master plan/coverage, client UI or another worktree.
Verification: Run scoped checks for changed files. Coordinate the heavy-job token before any full suite/native build. CLAUDE.md requires make verify after code changes and git diff --check plus actionlint .github/workflows/ci.yml before handoff; either obtain the token and run the required check or return DONE_WITH_CONCERNS explicitly awaiting coordinator integration verification, never declare it passed. An EPERM bind failure is sandbox-blocked; ask for appropriate scoped approval, never disable safeguards. Demonstrate multi-source approval and later update across CLI/MCP/bridge/desktop service paths, related and unrelated queries, workspace materialization and complete provenance. Include secondary protected-source canaries and malformed/reordered/duplicate/representative-ID negatives. Use synthetic fixtures and existing shared services.
Stop condition: Stop after a narrow tested local commit and report, or on interface/write-scope ambiguity. Do not push, open PRs, merge, rebase during active work, or start M2. No additional agents. Request coordinator integration when complete.
Sensitive-data policy: Synthetic Brain roots only; never use the default live Brain or copy databases, credentials, private captures, transcripts, source configuration or laptop environment folders. Backup/restore development is excluded. Optional local model downloads are permitted only in the later explicit bounded spike, not this initial research assignment. No service/OAuth/provider/release actions. Do not change security settings or use sandbox bypass.
Output contract: Write your own workstream STATE/HANDOFF using workflow-governance and report STATUS: DONE|DONE_WITH_CONCERNS|BLOCKED|sandbox-blocked; role and task IDs; base/HEAD; files changed; exact tests and results; commit SHA; risks; next action. Verify every artifact exists. Commit only owned changes using a message file and git commit -F. Never claim integrated or verified globally from a worker return.

Read relevant sources first. Work in small steps. Keep edits within scope. Report evidence, blockers, and next action. Stop on authority conflict, missing verification, scope conflict, or the stated stop condition.
