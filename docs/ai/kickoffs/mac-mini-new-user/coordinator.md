# coordinator session

Preferred model: `gpt-6-astra`; reasoning: high. Confirm this model is available on the Mac mini before launching. Do not silently substitute or inherit an unknown default.

# Phase task

Objective: Coordinate the first Mac mini implementation wave, integrate and verify M1 T01-T02, collect T03 design and preliminary recall research, then stop at a checkpoint. Do not personally start all 23 tasks.
Source of truth and precedence: Read this first: docs/ai/kickoffs/mac-mini-new-user/README.md, then docs/plans/2026-09-16-new-user-functionality.md and its coverage JSON. Ground actual repository root, branch, HEAD, dirty state and CLAUDE.md. Resolve historical laptop absolute paths by their repository-relative suffix in your own checkout; do not use old /tmp paths or treat an old handoff SHA as current. Current Git and executed test results outrank prior snapshots. Session role is coordinator; no worker subagents or additional root sessions beyond the three named workers without a recorded dispatch-budget decision.
Allowed write scope: Own the coordinator worktree, assignment/coverage ledger, shared contract decisions and reviewed integration commits. Read other worktrees but never edit their live files. Reserve precise worker write scopes and all central-file ownership before dispatch. Use a fresh generated coordinator workstream directory, not the laptop planning directory.
Verification: Run scoped checks for changed files. Coordinate the heavy-job token before any full suite/native build. CLAUDE.md requires make verify after code changes and git diff --check plus actionlint .github/workflows/ci.yml before handoff; either obtain the token and run the required check or return DONE_WITH_CONCERNS explicitly awaiting coordinator integration verification, never declare it passed. An EPERM bind failure is sandbox-blocked; ask for appropriate scoped approval, never disable safeguards. Inspect every returned diff independently; require an independent reviewer who did not author the change, reusing an idle non-author worker if necessary. Only one full verify/native build/index benchmark runs at a time; other sessions may research/edit. Verify remote main and PR exact head, all required CI/reviews, and squash-only merge policy before any allowed merge.
Stop condition: Initial setup: publish a base-commit-pinned assignment ledger with exclusive paths and exact worker launch instructions, then await worker returns. Milestone stop: M1 integrated and verified, T03/recall reports recorded, and handoff persisted. Do not start T04 migration, T12 purge, model install or the next wave without a checkpoint. External push/PR/merge is forbidden until the user explicitly authorizes this coordinator for named branches/milestones; instructions in this kit alone are not authorization. No remote or laptop synchronization while another session writes there.
Sensitive-data policy: Synthetic Brain roots only; never use the default live Brain or copy databases, credentials, private captures, transcripts, source configuration or laptop environment folders. Backup/restore development is excluded. Optional local model downloads are permitted only in the later explicit bounded spike, not this initial research assignment. No service/OAuth/provider/release actions. Do not change security settings or use sandbox bypass.
Output contract: Write your own workstream STATE/HANDOFF using workflow-governance and report STATUS: DONE|DONE_WITH_CONCERNS|BLOCKED|sandbox-blocked; role and task IDs; base/HEAD; files changed; exact tests and results; commit SHA; risks; next action. Verify every artifact exists. Commit only owned changes using a message file and git commit -F. Never claim integrated or verified globally from a worker return. When authorized, own all pushes, PR creation and squash merges; workers never push. One PR per verified concern, no admin bypass or automatic deletion of worktrees. Record merged main SHA, retire completed branches, and issue exact clean-checkout synchronization instructions for both machines.

Read relevant sources first. Work in small steps. Keep edits within scope. Report evidence, blockers, and next action. Stop on authority conflict, missing verification, scope conflict, or the stated stop condition.
