# contracts session

Preferred model: `gpt-6-astra`; reasoning: high. Confirm this model is available on the Mac mini before launching. Do not silently substitute or inherit an unknown default.

# Phase task

Objective: Design T03 shared contracts before adapter fan-out. Produce an implementation-ready contract document and synthetic compatibility fixtures, not a storage migration.
Source of truth and precedence: Read this first: docs/ai/kickoffs/mac-mini-new-user/README.md, then docs/plans/2026-09-16-new-user-functionality.md and its coverage JSON. Ground actual repository root, branch, HEAD, dirty state and CLAUDE.md. Resolve historical laptop absolute paths by their repository-relative suffix in your own checkout; do not use old /tmp paths or treat an old handoff SHA as current. Consume the coordinator's base SHA, reserved docs/fixture paths and current M1 interface. Resolve retrieval DTOs, explicit grants, version/error compatibility, immutable source/history identities, source ordering, route/generation semantics, cursor key lifecycle, untrusted content boundaries, migration/export parity and purge concurrency.
Allowed write scope: Only a coordinator-named new T03 design document, uniquely owned synthetic contract fixture paths and your generated workstream report. Read product files, but do not edit production contracts.py, schemas/migrations, CLI registries, current master plan or M1 tests. Document exact later file ownership rather than implementing downstream changes.
Verification: Check every proposed boundary against actual current code and version validators; distinguish recommended decisions from proven behavior. Check complete plan coverage and dependency sequencing. Run fixture/schema validation where an existing checker applies, git diff --check and applicable documentation checks. No full suite/native build unless assigned the heavy-job token. Return unresolved choices as explicit implementation gates, not silent assumptions.
Stop condition: Stop at a locally committed T03 contract proposal and fixture report awaiting independent review/coordinator freeze. Do not declare the interface frozen yourself. No schema migration, provider actions, push, PR, merge or nested agents.
Sensitive-data policy: Synthetic Brain roots only; never use the default live Brain or copy databases, credentials, private captures, transcripts, source configuration or laptop environment folders. Backup/restore development is excluded. Optional local model downloads are permitted only in the later explicit bounded spike, not this initial research assignment. No service/OAuth/provider/release actions. Do not change security settings or use sandbox bypass.
Output contract: Write your own workstream STATE/HANDOFF using workflow-governance and report STATUS: DONE|DONE_WITH_CONCERNS|BLOCKED|sandbox-blocked; role and task IDs; base/HEAD; files changed; exact tests and results; commit SHA; risks; next action. Verify every artifact exists. Commit only owned changes using a message file and git commit -F. Never claim integrated or verified globally from a worker return.

Read relevant sources first. Work in small steps. Keep edits within scope. Report evidence, blockers, and next action. Stop on authority conflict, missing verification, scope conflict, or the stated stop condition.
