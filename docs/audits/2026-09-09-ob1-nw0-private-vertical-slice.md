# NW0 private vertical-slice checkpoint

Status: independently reviewed private feasibility evidence. NW0 remains open; no shipping schema,
runtime, dependency or release change is included. Four launch paths remain required, with Codex
subscription deferred and Claude dispatch still closed at C7.

## Verified boundaries

A5 passes eight grouped tests for a standalone source-bound candidate, actual generic-kernel record
mapping, atomic private projection, abrupt process exits and idempotent recovery. The installed
importer rejects this candidate. Generic-kernel acceptance does not prove typed-schema enforcement
or installed retrieval.

B8 passes six source groups and eighteen native contract checks. Its unchanged content audit passes.
The bounded helper accepts selected Markdown bodies, negotiates capabilities and returns explicit
references. Missing targets cannot borrow another page's normalized identifier: targets require an
existing-file stamp joined to the selected inventory. Duplicate basenames conservatively block the
private snapshot. Full alias resolution, installed discovery, Linux and startup acceptance remain open.

Corrected C10 passes nine groups joining current consent, held input and bounded loopback transport
for the three direct-API wire shapes. Immutable route and source bindings prevent construction-time
routing changes. The output carries source context; this does not establish semantic quality.

A6 passes nine groups through real Engine capture/export, the private durable candidate, deterministic
inference, escaped evidence display, explicit link preview/acceptance and private export/import.
The same coordinator lock covers accepted mutations and authoritative source checks. Body, revision,
digest and privacy are read together at reservation, handoff and publication. A reproduced edit during
adapter construction initially allowed stale synthetic input to reach the loopback endpoint. The
corrected boundary checks again after construction and before the trusted held-input adapter starts.

Inference and preview preserve note bytes. Stale revisions, invalid excerpts, revoked policy and
owner impostors cannot accept a link. Completed retries return a durable receipt. Accepted links and
provenance survive cache removal, deletion, restart and restoration. Destination consent stays inactive.
The unchanged frozen helper returns zero explicit links before acceptance and the exact accepted pair
afterward. The resulting private export maps fifteen records through the real generic kernel.

The A5/B8/C10 reviewer reran 8/6/9 groups and checked native artifact hashes. A separate A6 review
reran all nine groups in 11.087 seconds and read the native receipts. Neither review found another
must-fix counterexample within its scope. No product inference calls ran. Native invocation durations
are functional diagnostics, not controlled cold/warm or five-minute acceptance measurements.

## Remaining gates and stop conditions

The proof assumes one trusted coordinator process. Installed portable schemas, conflict resolution,
retrieval, durable consent migration and cross-process writer authority remain unresolved. The evidence
page is a static inspection artifact, not Obsidian activation or navigation evidence. Runtime consent
still lives in the private A4 gate. Suggestions are disposable cache; accepted provenance is durable.

Official metadata preflight is not an inference, quality or retention-policy pass. Live adapters,
required platform/access samples and C7's supported Claude mechanisms remain open. Native Linux and
controlled cold starts are unproven. Isolated Mac and the agreed emulated Linux desktop still need
their actual journeys. Earlier Homebrew evidence does not certify the new helper's complete release.

The cumulative E allocation reaches its 120-minute stop limit at this checkpoint. Remaining A–D
allowances cannot silently replace review/decision-record effort. Preserve all existing receipts and
caps. Resume dependent work only when its missing capability and review capacity exist. No push,
publication, early NW1 or whole-NW0 completion is implied.

Related evidence: [A4/B7/C9](2026-09-09-ob1-nw0-a4-b7-c9-boundaries.md),
[B6 installation](2026-09-09-ob1-nw0-b6-resource-install.md),
[decision record](../plans/2026-09-09-ob1-native-workspace-nw0.md).
