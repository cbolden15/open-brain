# Core v0.1 T23 scoped macOS acceptance contract

Scope is T23/scoped A17 on local macOS arm64 only. Baseline b9f4a9cf944ecf1638b67c1af947407b561f0eee; artifact source 907bfe737b9366cafdb47ffb763f72df8dcf5f32. T21 implementation 987dea814a4b8730f8b3b73e19bb35262f6a6044. Product/runtime/schema fixes require a material scope decision; never repair the product within T23. No remote, live-source, OAuth, service or model operations.

Freeze before repository edits: add only tools/open_brain_dev/core_acceptance.py, tests/release/test_core_acceptance.py, docs/design/core-v01-t23-acceptance.md, bounded fixture/receipt contract docs/acceptance/core-v01-t23.json, T23 checkpoint and existing workstream state, and coverage/plan reconciliation. Preserve all 18 T21 reviewed file hashes and all product/build inputs (packages, release, Makefile, pyproject.toml, uv.lock). Schema 7/runtime 2 and completed desktop/Obsidian behavior are unchanged.

Acceptance matrix must identify row ID, scoped acceptance IDs, source commit, artifact/executable/archive and manifest hashes, platform, synthetic fixture identity, exact command or manual gate, expected and actual result, status, timing and explicit skip/open reason. Reused rows include original timing and receipt hash, never represented as newly executed. Final private receipt binds actual git HEAD, dirty paths, contract/harness hashes, input evidence hashes and UTC/monotonic times. Public evidence contains bounded sanitized counts/hashes only.

Frozen gate: uv run --frozen python -m tools.open_brain_dev.core_acceptance --root <absolute checkout> --release-dir <absolute build/native/release> --evidence-dir <absolute existing private run> --output <absolute private receipt>. Final gate requires clean git state; interim tests may report dirty but cannot certify it. Host must be macOS arm64. Verify exact T20 manifest and both archives/executables plus plugin assets, preserved T21 reviewed files and unchanged runtime source inputs before reuse. Use public pinned hashes/receipt schema, do not trust arbitrary passing JSON. Reuse T20 audit/Homebrew receipt and T21 committed 11-block artifact journey (CLI import/publication/vault/full Unicode reads, actual MCP grant catalog sessions, both generated nine-grant clients and removal, plugin assets staging/removal, four doctor checks) with hashes, required result fields and compatibility checks. Their original logs/receipts remain untouched. Do not rerun unchanged journeys or rebuild binaries without a demonstrated need.

New proof rows:
1. Hash-verified archive staging into a new disposable installed prefix (base bin, graphify libexec, plugin share assets). Reject unsafe archive members; no ambient installed executable/PYTHONPATH. Confirm same staged file hashes.
2. Empty HOME/cwd/default data, no preseed, credentials or inherited environment: allowlisted environment only; default init/capture/search/export verification using synthetic public canary; private root permissions; measure each new phase and total staging-to-export interval. The local offline clock is distinct from the historical 300-second GUI/provider clock; no GUI/provider timing pass claim.
3. Exact-artifact failure and redaction checks: no-grant MCP refusal, read-only MCP denied write with unchanged state, malformed request/error does not disclose synthetic secret/private path canaries, foreground subprocess EOF/exit bounded. Use controlled synthetic input; no real providers or credentials. Verify synthetic canary custody at explicitly expected product/export locations and absence from error/receipt surfaces; exported credentials/config/path are not permitted. Document exact scope, not hostile same-user isolation or production credential custody.
4. Gate integrity regressions reject wrong artifact/manifest/receipt/hash/source/platform, missing mandatory/reused row, premature success and canary leak. Tests use bounded synthetic fixtures, no rebuild.

Coverage reconciliation maps all A01-A17 to retained source evidence, new or reused artifact evidence, deferred/open remainder. A01-A07 source evidence is not blanket fresh native acceptance; A08-A13 deferred portions remain open; A14-A16 exact scoped evidence preserved; A17 local subset only. No global A17/A15, Linux, GUI, production provider/OAuth, live-source or public readiness claim. Catalog certification remains untouched. T10-T18/T22 and desktop GUI/release remain deferred; Obsidian GUI remains open and in scope.

Independent gpt-6-astra/high contract review before harness implementation; separate final and delta review in the same session, preserving original and superseding reports. Narrow gpt-5.6-sol/medium worker owns only new harness/tests/fixture receipt contract; coordinator owns reconciliation/state/design and integration. Shared ledger begins 8/12, max 3 active, no nesting, one heavy build. Atomic reservations/releases. Run focused tests, independent final review/repairs/delta, commit implementation with message file, serial make -j1 verify, git diff --check, actionlint .github/workflows/ci.yml and frozen clean exact-candidate gate. Commit sanitized checkpoint metadata separately using a message file; verify reviewed file hashes and runtime preservation, clean checkout and release children; private HANDOFF.json must equal final HEAD and pass validate-handoff.mjs. Safe fresh thread only after these conditions. Stop at this checkpoint; no next milestone.
