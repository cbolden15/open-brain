# Legacy implementation archive

This directory preserves the predecessor compatibility implementation and its tests. It is not a
uv workspace member, Python import root, test target, build target, or shipping dependency.

The files retain their original layout so their history remains available through Git. New Open
Brain or Secure Node code must not import `open_brain_legacy`.
