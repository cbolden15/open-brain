{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The renderer can invoke only the native D0 proof command.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
