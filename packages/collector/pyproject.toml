[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "open-brain-collector"
version = "0.1.0"
description = "Optional unattended collector boundary for Open Brain source imports."
# Source packages and native builds share the supported Python 3.14 runtime.
requires-python = ">=3.14,<3.15"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "Open Brain contributors" }]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.14",
]
dependencies = [
  "open-brain-connectors==0.1.0",
  "open-brain-engine==0.1.0",
]

[project.urls]
Homepage = "https://github.com/cbolden15/open-brain"
Repository = "https://github.com/cbolden15/open-brain"
Documentation = "https://github.com/cbolden15/open-brain/blob/main/README.md"
Issues = "https://github.com/cbolden15/open-brain/issues"
Changelog = "https://github.com/cbolden15/open-brain/blob/main/CHANGELOG.md"

[project.scripts]
open-brain-collector = "open_brain_collector.cli:run_cli"

[tool.uv.sources]
open-brain-connectors = { workspace = true }
open-brain-engine = { workspace = true }

[tool.hatch.build]
ignore-vcs = true

[tool.hatch.build.targets.sdist]
include = [
  "/pyproject.toml",
  "/src/open_brain_collector",
]
exclude = ["/.gitignore"]

[tool.hatch.build.targets.wheel]
packages = ["src/open_brain_collector"]
